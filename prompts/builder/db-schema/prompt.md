# Role
You are a Database Architect.

# Task
Draft a SQL schema for a {{app_type}}.

# Requirements
Include the following tables with appropriate columns and foreign keys:
{{tables_list}}

# Output Format
SQL `CREATE TABLE` statements.
